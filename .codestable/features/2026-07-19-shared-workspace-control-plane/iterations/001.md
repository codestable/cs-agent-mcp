---
doc_type: goal-iteration
feature: 2026-07-19-shared-workspace-control-plane
iteration: 1
status_after: active
next_action: "S1：以 RED 测试驱动 Broker 发现、认证、原子启动、stale recovery 与 idle lifecycle。"
blocker_signature: null
updated_at: 2026-07-19
---

# Iteration 001：S0 等价搬迁

## 当前理解

工作树已有 `workspace.ts`、`workspace-facade.ts` 和 `entrypoint.ts` 的纯搬迁。批准设计要求先证明
这次边界提取不改变单客户端 roots、Facade owner、进程锁、恢复和关闭语义，再引入 Broker。

## 实现方式

逐段对照基线 `3a5f14466a15c6b961dce45900fd0988c4f40964`，确认入口仅改为调用提取后的
`resolveRootWorkspace()` 与 `startWorkspaceFacade()`；随后只格式化三个既有改动文件。

## 本轮变更

- 保留并验证 Workspace resolver 与 RunningWorkspaceFacade factory 的已有纯搬迁。
- `entrypoint.ts` 继续维持原有 initialized、stdio disconnect 和资源关闭顺序。
- 未新增 Broker 行为，未改 13 tools、Facade snapshot v1、diagnostics v1 或 runtime 支持面。

## 验证证据

- `pnpm exec oxfmt --write`：三个 S0 文件格式化成功。
- `pnpm run typecheck`：通过。
- 定向 Node test：27/27 通过，覆盖 roots、restart、lock release/stale recovery 与 13-tool lifecycle。
- `pnpm run check`：226/226 通过；format、docs、typecheck、lint、build、test、pack dry-run 全绿。

## 遇到的问题

无行为差异或 blocker。CodeStable runtime 1.0.3 preflight 为 `ok`，知识图谱已为当前仓库建立索引。

## 下一次尝试

进入 S1，先写能证伪并发启动、descriptor 原子性、认证、stale recovery 与版本冲突的测试。

## 状态更新

S0 与 C01 标记为 `done`；goal 保持 `running / implementation`。
