---
doc_type: goal-iteration
feature: 2026-07-19-shared-workspace-control-plane
iteration: 7
status_after: complete
next_action: "等待 owner 明确指令后再执行 commit、push、tag、版本升级或 npm 发布。"
blocker_signature: null
updated_at: 2026-07-19
---

# Iteration 007：真实双 Host 验收与 Goal 完成

## 当前理解

Iteration 006 后唯一缺口是 C16。owner 明确本机 Codex/Claude 正常后，需要区分用户登录状态与当前
Agent shell 注入环境，并避免 Codex 同时加载全局 `cs-agent` 和临时当前构建造成自争锁。

## 实现方式

不修改代码。只对验收子进程 unset 注入的 Codex API 环境变量，保留用户本机登录；Codex 禁用全局
`cs-agent` 并只加载当前 `dist/mcp-cli.js`，Claude 使用 strict MCP config。先验证跨 Host lifecycle，
再用空闲 Codex TUI 根作为确定性连接屏障做严格并行复验。

## 本轮变更

- 没有实现代码变更。
- 完成真实 Codex/Claude C16 与严格并行 lease/lock 证据。
- 独立功能验收从 `inconclusive` 更新为 `pass`。
- S6/C16 更新为 done；Goal、QA、acceptance 与 protocol 更新为 complete/passed。
- 原 blocker approval 标为 superseded，历史 Iteration 004-006 保留。

## 验证证据

- `env -u CODEX_API_KEY -u OPENAI_API_KEY codex exec --ephemeral`：Codex 0.144.5、provider
  `sub2api` 返回 `OK`；Claude 2.1.186/Opus 4.8 真实调用当前 MCP 成功。
- Codex 创建 managed Agent `b6c45bd8-60c3-4863-a1f3-9be0b94beb21`；Claude 跨根 list/status/send/
  cancel/wait/destroy，Turn `a8f4a7fd-e82b-4b33-8922-2c65c41ab4e6` 最终为 cancelled。
- 严格并行中 Codex interactive 根持续连接；Claude 跨根 status/destroy Agent
  `7554290b-f2ee-4a0c-b2fc-48c77c6eaa93`。前、中 Broker pid 均为 27871，lock token 均为
  `052072e9-5332-435b-b6b8-a7c2f2f1bb0c`。
- Codex 根正常退出后 diagnostics 为 `agents=[]`、pid 27871 退出、Broker descriptor 与 Workspace
  lock 释放。
- 最终 fresh `pnpm run check`：246/246 通过，pack dry-run 成功。
- 独立 Task agent `/root/shared_workspace_acceptance`：C16、场景 15、17 个场景、C01-C18 与 overall
  functional acceptance 全部 `pass`，无 blocking/important gap。

### Owner-facing tarball 复验

- 使用 `npm pack` 产物 `/tmp/cs-agent-mcp-owner-pack.npP0l5/cs-agent-mcp-0.2.3.tgz` 临时安装到
  `/tmp/cs-agent-mcp-owner-prefix.w0VYNd`，未覆盖用户全局安装或 MCP 配置。
- Codex 创建 Agent `c66bdebe-2b64-4fd3-b6b0-5cb36fbcb2eb` 与 Turn
  `2e2922b0-9204-49b4-b3de-25c1b6d48888`；Claude 看到相同 ID 并完成 status/wait/destroy。
- Claude 退出后，仍连接的 Codex 成功创建、list/status 并销毁第二个 Agent
  `831bee3f-7a74-41b0-81b7-e6db7e74bbd5`，证明单个 Host 离开不影响另一根连接。
- 专用取消样本 Agent `576b93b3-0a03-47c9-82ed-427cf84e2e3b`、Turn
  `3eaf8065-c5a6-4e5b-a2a4-570a080f1044`：Claude cancel 后 wait 得到 `cancelled` revision 12、
  `stopReason=cancelled`，随后 destroy；Codex 同时观察到相同终态。
- tarball 真实 PTY 的 `agents top` 首页、Agent attach 事件流和 `q` 终端恢复通过；自动化
  `pnpm run test:tui-e2e` 返回 top/ps/keyboard/SGR mouse/resize/attach/terminal restore 全部 `ok`。
- 两个 Host 退出后，临时 binary 的 `agents list --json` 返回 `agents=[]`、`warnings=[]`；进程表中
  无该 tarball Broker，当前 Workspace 的 Broker descriptor 与 lock 均已清理。
- 首个专用取消尝试误把 `timeoutMs=30000` 设为 Turn 总超时，样本先进入 `failed/TIMEOUT`；该样本
  未计为通过，并已销毁。随后以上述 600 秒样本按明确 ID 重跑，得到真实 `cancelled` 终态。

## 遇到的问题

当前 Agent shell 注入的 API key 会遮蔽用户本机 Codex auth；Codex 同时加载全局 `cs-agent` 与临时
`c16` 会启动两个 MCP server 并自争 Workspace lock。两者均通过验收子进程隔离解决，没有修改用户
credential 或全局配置，也不是产品实现缺陷。

## 下一次尝试

无。所有 acceptance signal 已满足。commit、push、tag、版本升级与 npm 发布仍等待 owner 明确指令。

## 状态更新

S0-S6、C01-C18、独立 code review、QA 与功能验收全部通过；Goal 状态更新为 complete。
最终验收见 `shared-workspace-control-plane-acceptance.md`。
