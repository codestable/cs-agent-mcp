---
doc_type: goal-iteration
feature: 2026-07-19-shared-workspace-control-plane
iteration: 4
status_after: blocked
next_action: "等待 owner 刷新 Codex credential 并正常关闭旧 MCP Host；随后仅重跑 C16。"
blocker_signature: c16-real-host-environment-unavailable
updated_at: 2026-07-19
---

# Iteration 004：最终加固与严格 Owner Stop

## 当前理解

最后的实现风险集中在跨进程 generation 判定、stale recovery TOCTOU 与 unresolved roots 跨越
grace。完成这些修复后，只有真实 Codex/Claude Host 环境能决定 C16。

## 实现方式

先建立能打破旧实现的跨时区与双 contender 真实子进程测试，再串行化 lock mutation；为 unresolved
root session 增加 initialization hold，并用 1500ms roots 延迟跨过 750ms grace 验证原 token。

## 本轮变更

- `processIdentity` 固定 `LC_ALL=C`、`TZ=UTC`。
- sidecar mutation guard 串行化 lock acquire、release 与 stale recovery。
- unresolved root session 暂停已有 Workspace grace，所有终止路径幂等释放 hold。
- 补齐关闭顺序、shutdown 失败传播与 diagnostics/TUI/tarball 证据。
- 独立 reviewer 的 blocking/important 全部闭合，最终 code review `passed`。

## 验证证据

- `pnpm run check`：246/246 通过。
- 独立定向复验：62/62 通过，53.68 秒。
- 跨时区 contender 与双 stale contender 真实子进程测试通过，严格一胜一败。
- roots 延迟 1500ms 跨过 750ms grace，Broker pid 与 Workspace lock token 不变。
- TUI PTY 全部 `ok`；真实 tarball 临时安装和 MCP SDK smoke 为 13 tools、lifecycle/diagnostics `ok`。
- 独立代码审查 `passed`；独立功能验收对场景 1-14、16-17 判定 pass。

## 遇到的问题

第三次 fresh Codex 调用仍由 provider 返回 401 `INVALID_API_KEY`；用户既有 pid 43619 继续持有当前
仓库旧 Workspace lock。安全边界不允许自动修改 secret 或终止用户 Host，所以场景 15 / C16 仍为
inconclusive。同一 blocker 已连续三轮复现，命中 strict owner-stop。

## 下一次尝试

owner 刷新 Codex provider credential，并正常关闭或重启旧 MCP Host。恢复后仅重跑 C16；通过则
更新验收为 passed 并完成 Goal，否则根据 fresh evidence 继续最小修复。

## 状态更新

S0-S5、C01-C15、C17-C18 为 done；S6/C16 为 blocked。Goal 进入 handoff，不 commit、push、tag、
发布或升级版本。功能验收见 `shared-workspace-control-plane-acceptance.md`。
